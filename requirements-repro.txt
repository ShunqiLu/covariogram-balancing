# The counters themselves have no third-party runtime dependencies.
# These versions pin the verification tools used for the archived run.
pytest==9.1.1
mypy==1.14.1
# Figure regeneration in the full mode, and the figure geometry check.
matplotlib==3.10.0
pypdf==6.5.0
